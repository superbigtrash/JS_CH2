함수를 정의하는 세 가지 방식에 대해 알아보는 예제
function 문자 (인자) { 내용 } 과 같이 쓰게 되면
함수선언문이고, 함수명 문자가 곧 변수명이된다.

var 변수명 = function () { }과 같이 쓰게되면
익명 함수표현식이며 변수명이 곧 함수명이 된다.

두번째와 같이 선언할때 함수뒤에 이름을 지정해주는 경우
기명 함수식이라 하고 변수명과 함수명이 같거나 달라지게 할 수 있다.
주의점으로는 변수명으로 함수를 실행시키는 것이지, 함수명으로는 실행시키지 못한다.
